/**
 * In this section, we will learn about the if-else statement in JavaScript.
 * The if-else statement is a control flow statement that allows you to execute different blocks of code depending on a certain condition.
 */

let age = 20;

if(age >= 18) {
    console.log("You are eligible to vote.");
} else {
    console.log("You are not eligible to vote.");
}